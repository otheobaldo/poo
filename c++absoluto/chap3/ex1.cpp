#include <iostream>
#include <cmath>
#include <cstdlib>
using namespace std;

int main()
{
    // cout << sqrt(16.0) << endl << sqrt(16) << endl << pow(2.0, 3.0) << endl;
    int x, y, area, fudge, a, b, c, tide, nobody, time;
    cout << sqrt(x + y) << endl;
    cout << pow(x, (y+7)) << endl;
    cout << sqrt(area + fudge) << endl;
    cout << sqrt(time + tide)/nobody << endl;
    cout << (-b + ((sqrt(pow(b, 2)-(4*a*c)))/2*a)) << endl;
    cout << 5 + (rand()%6) << endl;


    return 0;
}