#include <iostream>
using namespace std;

int main()
{
    int contagem = 0, limite = 10;

    //a
    cout << (contagem == 0) && (limite < 20);
    // true

    //b
    cout << contagem == 0 && limite < 20;
    // false
    /*c = true
    d = true
    e = false
    f = true
    g = false
    h = erro
    i = true
    j = erro
    k = false
    l = 1*/

    //2 
    // (x > 2) && (x < 3)


    return 0;
}